%h1 Listing promotions

%table.table
  %thead
    %tr
      %th Name
      %th Description
      %th Starting date
      %th Ending date
      %th Discount %
      %th Product
      %th
      %th
      %th
  %tbody
    - promotions.each do |promotion|
      %tr
        %td= promotion.name
        %td= promotion.description
        %td= promotion.start_date.to_formatted_s(:short)
        %td= promotion.end_date.to_formatted_s(:short)
        %td= promotion.modifier
        %td= promotion.product.name
        %td= link_to 'Show', city_promotion_path(city,promotion), class: 'btn btn-default'
        - if current_user.present? && current_user.admin?
          %td= link_to 'Edit', edit_city_promotion_path(city,promotion), class: 'btn btn-default'
          %td= link_to 'Delete', city_promotion_path(city,promotion), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default'
        - else
          %td
          %td

